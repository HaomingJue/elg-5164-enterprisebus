server:
  port: 8080

#management:
#  metrics:
#    export:
#      prometheus:
#        enabled: true
#  endpoints:
#    web:
#      exposure:
#        include: '*'
management:
  enable:
    process:
      cpu:
        usage: true

  endpoints:
    web:
      exposure:
        include: '*'
  prometheus:
    metrics:
      export:
        enabled: true